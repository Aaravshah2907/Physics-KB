#!/bin/bash
# pkb-search: Index-based search with focused viewing using bat

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/pkb-common.sh"

INDEX_JSON="$PROJ_ROOT/topic_index.json"

# Check dependencies
if ! command -v jq &> /dev/null; then
    echo "❌ Error: jq is required. Install with: brew install jq"
    exit 1
fi

if ! command -v bat &> /dev/null; then
    echo "❌ Error: bat is required. Install with: brew install bat"
    exit 1
fi

if ! command -v fzf &> /dev/null; then
    echo "❌ Error: fzf is required. Install with: brew install fzf"
    exit 1
fi

# Ensure index exists
if [ ! -f "$INDEX_JSON" ]; then
    echo "⚠️  Index not found. Generating..."
    "$SCRIPT_DIR/pkb-index"
fi

QUERY="$1"

# Function to display topic with bat at specific line
view_topic() {
    local file="$1"
    local line="$2"
    local title="$3"
    
    echo -e "\n${BLUE}📖 Viewing: $title${RESET}"
    echo -e "${CYAN}   File: $file (line $line)${RESET}\n"
    
    # Use bat with line highlighting
    bat --color=always \
        --style=numbers,grid \
        --highlight-line "$line" \
        --line-range "$line:" \
        "$NOTES_DIR/$(basename "$file")"
}

# Browse Mode (No Query) - Show all topics hierarchically
if [ -z "$QUERY" ]; then
    echo -e "${CYAN}📚 Browse Physics KB Topics${RESET}"
    echo ""
    
    # Build searchable list: "File > Topic Path (line X)"
    TOPICS=$(jq -r "$JQ_FLATTEN_FILTER"'
    .files[] | 
    .hierarchy[] as $topic |
    flatten_topics(.title; .path) |
    .file + " │ " + .path + " │ L" + (.line|tostring)
    ' "$INDEX_JSON")
    
    # Use fzf for selection with preview
    SELECTED=$(echo "$TOPICS" | fzf \
        --height 80% \
        --layout=reverse \
        --border \
        --prompt="🔍 Search topics: " \
        --preview-window=right:60% \
        --preview 'FILE=$(echo {} | cut -d"│" -f1 | xargs); LINE=$(echo {} | cut -d"│" -f3 | sed "s/L//"); bat --color=always --style=numbers --highlight-line $LINE --line-range $((LINE > 5 ? LINE - 5 : 1)):$((LINE + 20)) "'"$NOTES_DIR"'/$FILE"')
    
    if [ -n "$SELECTED" ]; then
        FILE=$(echo "$SELECTED" | cut -d'│' -f1 | xargs)
        TITLE=$(echo "$SELECTED" | cut -d'│' -f2 | xargs)
        LINE=$(echo "$SELECTED" | cut -d'│' -f3 | sed 's/L//' | xargs)
        
        view_topic "$FILE" "$LINE" "$TITLE"
    fi
    exit 0
fi

# Search Mode - Find topics matching query
echo -e "${CYAN}🔍 Searching for: '$QUERY'${RESET}"
echo ""

# Search in topic titles and file content
MATCHING_TOPICS=$(jq -r --arg query "$QUERY" "$JQ_FLATTEN_FILTER"'
.files[] | 
.hierarchy[] as $topic |
flatten_topics(.title; .path) |
select(.title | ascii_downcase | contains($query | ascii_downcase)) |
.file + " │ " + .path + " │ L" + (.line|tostring)
' "$INDEX_JSON")

if [ -z "$MATCHING_TOPICS" ]; then
    echo "❌ No topics found matching '$QUERY'"
    echo ""
    echo "💡 Searching file content instead..."
    
    # Fall back to grep search
    GREP_RESULTS=$(grep -rn "$QUERY" "$NOTES_DIR"/*.md 2>/dev/null | head -20)
    
    if [ -z "$GREP_RESULTS" ]; then
        echo "❌ No results found in file content either."
        exit 1
    fi
    
    # Format grep results for fzf
    SEARCH_RESULTS=$(echo "$GREP_RESULTS" | while IFS=: read -r file line content; do
        filename=$(basename "$file")
        echo "$filename │ $content │ L$line"
    done)
    
    SELECTED=$(echo "$SEARCH_RESULTS" | fzf \
        --height 80% \
        --layout=reverse \
        --border \
        --prompt="📄 Content matches: " \
        --preview-window=right:60% \
        --preview 'FILE=$(echo {} | cut -d"│" -f1 | xargs); LINE=$(echo {} | cut -d"│" -f3 | sed "s/L//"); bat --color=always --style=numbers --highlight-line $LINE --line-range $((LINE > 5 ? LINE - 5 : 1)):$((LINE + 20)) "'"$NOTES_DIR"'/$FILE"')
    
    if [ -n "$SELECTED" ]; then
        FILE=$(echo "$SELECTED" | cut -d'│' -f1 | xargs)
        CONTENT=$(echo "$SELECTED" | cut -d'│' -f2 | xargs)
        LINE=$(echo "$SELECTED" | cut -d'│' -f3 | sed 's/L//' | xargs)
        
        view_topic "$FILE" "$LINE" "$CONTENT"
    fi
else
    echo -e "${GREEN}✓ Found matching topics${RESET}"
    echo ""
    
    # Use fzf for selection
    SELECTED=$(echo "$MATCHING_TOPICS" | fzf \
        --height 80% \
        --layout=reverse \
        --border \
        --prompt="📑 Select topic: " \
        --preview-window=right:60% \
        --preview 'FILE=$(echo {} | cut -d"│" -f1 | xargs); LINE=$(echo {} | cut -d"│" -f3 | sed "s/L//"); bat --color=always --style=numbers --highlight-line $LINE --line-range $((LINE > 5 ? LINE - 5 : 1)):$((LINE + 20)) "'"$NOTES_DIR"'/$FILE"')
    
    if [ -n "$SELECTED" ]; then
        FILE=$(echo "$SELECTED" | cut -d'│' -f1 | xargs)
        TITLE=$(echo "$SELECTED" | cut -d'│' -f2 | xargs)
        LINE=$(echo "$SELECTED" | cut -d'│' -f3 | sed 's/L//' | xargs)
        
        view_topic "$FILE" "$LINE" "$TITLE"
    fi
fi
